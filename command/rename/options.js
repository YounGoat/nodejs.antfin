'use strict';

const MODULE_REQUIRE = 1
	/* built-in */
	
	/* NPM */
	
	/* in-package */
	;

module.exports = {
	"options": [
		'--help -h NOT ASSIGNABLE',
		'--old [0] NOT NULL REQUIRED',
		'--new [1] NOT NULL REQUIRED',
	]
};