'use strict';

const MODULE_REQUIRE = 1
	/* built-in */
	
	/* NPM */
	
	/* in-package */
	;

module.exports = {
	"options": [
		'--help -h NOT ASSIGNABLE',
		'--name -n NOT NULL',
		'--usage NOT NULL',
		'--pattern --command [0] NOT NULL',
	]
};