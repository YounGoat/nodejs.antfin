'use strict';

const MODULE_REQUIRE = 1
	/* built-in */
	
	/* NPM */
	
	/* in-package */
	;

module.exports = {
	"options": [
		'--help -h NOT ASSIGNABLE',
		'--force -f NOT ASSIGNABLE',
		'--name -n [0] NOT NULL',
		'--search -s NOT NULL',
	]
};